# 屏幕翻译工具依赖库

# 屏幕截图
pyautogui>=0.9.54

# 图像处理
Pillow>=10.0.0

# 音频播放
pyaudio>=0.2.14

# OpenAI API客户端（用于通义千问）
openai>=1.0.0

# 阿里云DashScope SDK（用于语音合成）
dashscope>=1.14.0

# 键盘监听（用于快捷键）
keyboard>=0.13.5

# 听歌识曲
shazamio>=0.4.0
audioop-lts>=0.2.0

# 系统音频录制
soundcard>=0.4.3
soundfile>=0.12.1
numpy>=1.24.0

# Windows WASAPI 环形回录（支持蓝牙耳机）
pyaudiowpatch>=0.3.0