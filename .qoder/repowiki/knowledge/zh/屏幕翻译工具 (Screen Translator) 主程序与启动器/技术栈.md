Python 3.8+ / Tkinter GUI；OCR 未显式引入本地引擎，翻译走阿里云 DashScope（OpenAI 兼容接口）；语音合成使用 `dashscope` 的 CosyVoice v3-flash 流式 HTTP TTS；音频录制三套回退（`soundcard` → `pyaudiowpatch` WASAPI 环回 → `pyaudio` 立体声混音），歌曲识别用 `shazamio`（需 Rust 编译环境）。